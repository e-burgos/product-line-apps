{
  "name": "agro-swap-frontend",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/frontend/src",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project agro-swap-frontend --web",
  "targets": {
    "install": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["npm install --prefix apps/frontend"]
      }
    }
  }
}

export type BudgetFormData = {
  title: string;
  description: string;
};

export type BudgetDetailsFormData = {
  budgetId: string;
  title: string;
  description: string;
  quantity: number;
  amount: number;
  productId: string;
};
